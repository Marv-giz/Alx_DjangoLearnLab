# LibraryProject

A basic Django project setup serving as a foundation for developing a library management application. This project demonstrates the default Django structure and configuration.

## Prerequisites

Before running this project, ensure you have Python installed globally on your machine.

## Setup & Installation

Since this project does not use a virtual environment, simply install Django globally if you haven't already:

```bash
pip install django